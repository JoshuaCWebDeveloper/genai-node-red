export type FlowEditorProps = Record<string, never>;

// eslint-disable-next-line no-empty-pattern
export const FlowEditor = ({}: FlowEditorProps) => {
    return <div>{/* Empty React Component */}</div>;
};

export default FlowEditor;
